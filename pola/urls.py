# -*- coding: utf-8 -*-
from django.conf.urls import url

from . import views


urlpatterns = [
    # url(r'product/(?P<slug>[-\w]+)/$',
    #     views.ProductDetailView.as_view(), name="product-detail"),
    # url(r'product/(?P<slug>[-\w]+)/edit$',
    #     views.ProductUpdate.as_view(), name="product-edit"),
    # url(r'product/(?P<slug>[-\w]+)/delete$',
    #     views.ProductDelete.as_view(), name="product-edit"),
    # url(r'product/$',
    #     views.ProductListView.as_view(), name="product-list"),
    # url(r'product/empty$',
    #     views.ProductListView.as_view(), name="product-list-empty"),
    # url(r'product/(?P<code>[-\w]+)/history$',
    #     views.product_edit_history, name="product-edit-history"),
    # url(r'product/create$',
    #     views.ProductCreate.as_view(), name="product-create"),

    # url(r'product/(?P<code>[-\w]+)/image$',
    #     views.get_image, name="product-image"),


]
